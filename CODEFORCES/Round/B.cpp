#include <cstdio>
using namespace std;
int main() {
    int n, l, r, lnum, rnum;
    scanf("%d", &n);
    if (n % 4 != 0) {
        printf("! -1\n");
        fflush(stdout);
        return 0;
    }
    printf("? %d\n", 1);
    fflush(stdout);
    scanf("%d", &lnum);
    printf("? %d\n", n);
    int l =
    return 0;
}