#include <bits/stdc++.h>
using namespace std;
int main() {
    // freopen("in.txt", "r", stdin);
    int T;
    long long n;
    scanf("%d", &T);
    while (T--) {
        scanf("%lld", &n);
        printf("%lld\n", n - 1);
    }
    return 0;
}